# GS2 Input

This folder contains only the user-provided clean base ROM.

Required user-provided file:

- `golden_sun_2_eng.gba` — clean English Golden Sun 2 ROM

Original ROM files are never modified in place.

Bundled build resources live in `../code/resources/`.
Editable translations and generated files live in `../output/`.
